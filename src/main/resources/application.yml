spring:
  datasource:
    url: jdbc:mysql://localhost:3304/db01
    username: root
    password: 123456
    driverClassName: com.mysql.cj.jdbc.Driver
  data:
    redis:
      database: 0
      host: localhost
      port: 8379
server:
  port: 9090
mybatis:
  mapper-locations: classpath:**/mapper/*.xml
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
